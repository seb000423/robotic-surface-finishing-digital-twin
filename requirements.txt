# ===========================================================================
# 자동차 본네트 폴리싱 시뮬레이션 - Python 의존성
# ===========================================================================
#
# 아래 패키지는 시스템 python3(3.10) 환경에서 실행되는 스크립트
# (path_generator.py, 각종 변환/유틸 스크립트, web_dashboard/tools)에 필요합니다.
#
#   pip install -r requirements.txt
#
# 버전은 개발 당시(Python 3.10 / Ubuntu 22.04) 기준 동작 확인된 하한입니다.
# ---------------------------------------------------------------------------

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
Pillow>=9.5
gmsh>=4.11          # STEP -> OBJ/USD 변환 (convert_step.py, step_to_obj.py)

# ===========================================================================
# 아래 모듈은 pip로 설치하지 않습니다 (Isaac Sim / ROS2 가 제공).
# ===========================================================================
#   isaacsim, omni.*, carb, pxr   -> NVIDIA Isaac Sim 내장
#                                    ( isaac_python 으로 실행 시 자동 사용 )
#   rclpy, std_msgs, sensor_msgs  -> ROS2 Humble (/opt/ros/humble)
#
# Isaac Sim 스크립트(scan.py, polishing_v*.py)는 반드시 시스템 python3 가 아닌
# isaac_python( python.sh ) 으로 실행해야 합니다.
# ===========================================================================
